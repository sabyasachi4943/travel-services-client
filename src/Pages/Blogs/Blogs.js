import React from 'react';

const Blogs = () => {
  return (
    <div>
      Difference between SQL and NoSQL What is JWT, and how does it work? What
      is the difference between javascript and NodeJS? How does NodeJS handle
      multiple requests at the same time?
    </div>
  );
};

export default Blogs;